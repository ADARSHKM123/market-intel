# Database
DATABASE_URL=postgresql://postgres:password@localhost:5432/market_intel

# Redis
REDIS_HOST=localhost
REDIS_PORT=6379

# JWT
JWT_SECRET=your-super-secret-jwt-key-change-in-production
JWT_EXPIRES_IN=7d

# Backend
BACKEND_PORT=3001
NODE_ENV=development

# NLP Service
NLP_SERVICE_URL=http://localhost:8001

# YouTube Data API v3
YOUTUBE_API_KEY=AIzaSyB4S_LTDxTU7CMdWNhF3vZWF3kZtG_OenM
YOUTUBE_DAILY_QUOTA=10000

# Hacker News API — no key needed (free public API)
# Dev.to API — no key needed (free public API)
# GitHub Search API — no key needed (public, 10 req/min limit)
# Product Hunt — no key needed (uses public frontend API)

# Optional: Email alerts
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
ALERT_FROM_EMAIL=
